{
  "name": "guardian-fund-demo",
  "version": "0.1.0",
  "description": "Guardian Fund eligibility & remediation calculation — technical demo (from-scratch recreation, no proprietary code)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
